#include <iostream>
int main()
{
	float y = 1.4; int m = 6; double z = 0.05E-5, w, y;
	y = sqrt(1+x) - cos(2/m);
	w = 0.6 * z / 2 * exp((-2) * y * m);
	std::cout << "V resultate rasscheta po formule y=" << y;
	std::cout << "V resultate rasscheta po formule w=" << w;
}